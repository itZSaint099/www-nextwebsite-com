import styled from 'styled-components';
import { Container } from 'react-bootstrap';

export const LibraryWrapper = styled(Container)`
  padding-top: 2rem;
`;

export const DocsWrapper = styled.div`
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
`;
